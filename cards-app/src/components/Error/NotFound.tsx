import React, { ReactElement } from 'react';

const NotFound: React.FC<unknown> = (): ReactElement => {
    return (
        <div>
            <span>Page not found</span>
        </div>
    );
};

export default NotFound;
